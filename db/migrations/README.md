# Schema migrations

Applied to the Supabase project `finance-tracker` (ref `anpvflizwcmyefbxsfpx`).
Kept here so the schema history lives with the app rather than only in the
Supabase dashboard. Files are named `<timestamp>_<name>.sql` and are already
applied — this folder is a record, not a pending queue.

To apply a new one: add the file here *and* run it (Supabase SQL editor or MCP),
so the two never drift.
